Note

Local variable inside function ≠ global variable outside.

Key Takeaways
Variables inside a function are local (scope )
Variables outside are global
Changing a local variable does NOT change the global one
Python treats variables separately based on scope